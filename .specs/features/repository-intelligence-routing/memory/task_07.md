# T7 routed repository-intelligence QA memory

- Deep Review QA now treats Graft as default and Graphify as one-question-on-demand; all tool failures
  retain explicit degraded inspection and source authority.
- Adoption QA covers ignored `graft/`, `graphify-out/`, and `.repository-intelligence/` state without
  changing runtime dependencies.
- The retention scenario requires matched controls and 10–20 distinct terminal tasks before an
  explicit keep/remove decision.
