# Release 0.11.0 QA Execute

- **Date:** 2026-09-11
- **Tree:** `5964808421b5b47b7c73292d678a5aa49411b58c`
- **Adapter:** CLI/manual
- **Execution path:** checkout-local disposable Git fixtures, local package dry-run, fake Graphify/Graft binaries, filesystem readback
- **Environment:** macOS checkout; Python 3; Bun 1.4.x authority from `package.json`
- **Opening gate:** `bun run test:all` — pass, exit 0 after one evidence-wrapper retry; product command itself did not fail
- **Closing gate:** `bun run test:all` — pass, exit 0; 846 passed, 0 failed
- **Raw evidence:** `docs/qa/evidence/2026-09-11-release-0-11-0/`
- **Limitations:** no browser/API/mobile/runtime surface; no external tool install, real benchmark task, Deep Review cohort, registry/GitHub request, tag, publish, push, or merge

## Matrix

| Charter | Scenario | Verdict | Independent confirmation | Evidence |
| --- | --- | --- | --- | --- |
| `CH-configure-on-demand-deep-review-2026-09-11` | `CFG-resolve-deep-review-cadence` | pass | Resolver reload returned the same frozen `skip`/`[]`; template hashes stayed equal after packet sync | `cadence-summary.txt`; `cadence-default-reload.json`; `cadence-invalid.stderr`; `remediation-invalid.stderr`; `packet-sync.json`; `packet-routing-readback.txt` |
| `CH-adopt-repository-intelligence-2026-09-11` | `ADP-report-repository-intelligence-setup` | not completed — stopped on adjacent product defect | Fresh install printed both pins and installed router bytes, but re-adoption was not run | `adoption-install.log`; `adoption-installed-files.sha256`; `adoption-ignore-check.txt` |
| `CH-adopt-repository-intelligence-2026-09-11` | `CFG-keep-local-artifacts-out-of-git` | not completed — stopped on adjacent product defect | Hypothetical generated paths resolve to installed ignore rules; clean-clone regeneration was not run | `adoption-ignore-check.txt` |
| `CH-route-deep-review-intelligence-2026-09-11` | `QAS-use-graft-context-with-plain-fallback` | not run — session stopped on product defect | n/a | n/a |
| `CH-decide-repository-intelligence-retention-2026-09-11` | `QAS-retain-routed-repository-intelligence` | not run — session stopped on product defect | n/a | n/a |
| `CH-review-release-0-11-0-2026-09-11` | `DOC-use-optional-tools-with-repository-authority` | not run — session stopped on product defect | n/a | n/a |
| `CH-review-release-0-11-0-2026-09-11` | `REL-report-current-workflow-release` | not run — session stopped on product defect | n/a | n/a |

## Adjacent canaries

| Scenario | Entering state | Result | Evidence |
| --- | --- | --- | --- |
| `ADP-install-phase-skills` | pass | fail — `BUG-20260911-packaged-install-omits-claude-skill-links` | `adoption-install.log`; `adoption-phase-pointer-failure.txt`; `adoption-link-counts.txt` |
| `QAS-resolve-phase-skill-procedures` | pass | not reached; preserved | n/a |
| `ADP-install-versioned-workflow-package` | pass | contradiction observed through same packaged install; not reset separately because the failed canonical owner is `ADP-install-phase-skills` | `adoption-install.log`; `adoption-link-counts.txt` |
| `CFG-centralize-agent-model-routing` | pass | pass canary during cadence charter | `packet-sync.json`; `packet-routing-readback.txt` |
| `QAS-run-one-job-remediation-check` | pass | not reached; preserved | n/a |

## Session log

### Configure on-demand Deep Review

Default and reload returned `cadence: skip`, `groups: []`, and live `stall_attempts: 3`. Explicit
`slice`, `feature`, and `grouped.2` produced `[[1],[2]]`, `[[1,2]]`, and `[[1,2]]`. Invalid cadence
and negative remediation values exited `2` before creating snapshots. Packet synchronization changed
only `.codex/agents/verifier.toml` model/effort metadata; tracked template hashes remained equal and
Graphify/Graft routing prose survived in generated packets.

First disposable setup attempt omitted `.my-workflow.toml`; second exposed the documented prerequisite
to synchronize generated provider packets before resolution. Both stopped before feature state. The
completed walk followed the public sync-first contract.

### Adoption stop

Fresh offline installation from the exact local `0.11.0` tarball exited `0`, printed
`@nanonets/graft@0.10.1` and `graphifyy==0.9.14` as manual commands, preserved `package.json` and the
consumer-owned `.my-workflow.toml`, installed exact router/guide bytes, and installed ignore rules
for all three generated intelligence roots. The installed target also contained the five canonical
phase skills.

Independent pointer readback then failed: all five `.claude/skills/w<phase>` links were absent.
Archive inspection found zero `.claude/skills/` members versus 19 source-checkout symlinks. Filed
`BUG-20260911-packaged-install-omits-claude-skill-links` and stopped before re-adoption or later
charters, as required by the QA fix loop.

## Cycle disposition

**FAIL / suspended for remediation.** One mapped scenario passed, one adjacent canary failed, six
mapped scenarios remain `untested`; one charter completed, one stopped partway, and three were not
run. One Major product defect was filed. No product code changed.

The closing gate passed 846 checks with zero failures: 126 Bun tests, 195 Node installer tests, and
525 Python checks. This green source-tree gate does not contradict the package-path defect.

`npm pack --dry-run`, re-adoption, fake Graphify/Graft routing, benchmark fixtures, release identity,
package-member byte comparison, and later adjacent canaries were not executed after the required
defect stop. No external tool, reviewer cohort, live model, registry, GitHub, tag, publication,
push, merge, browser, API, mobile, or product runtime was used.

All three checkout-owned disposable targets were removed. Root archive residue and residual target
counts were both zero; the local tarball remains only inside ignored raw evidence.
